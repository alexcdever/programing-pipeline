---
name: programing-pipeline
description: "Use when an AI agent plans, implements, reviews, and merges coding work."
version: 0.1.0
author: Alex Chen (alexcdever)
license: MIT
platforms: [linux, macos, windows]
metadata:
  tags: [coding, workflow, planning, testing, review, worktree, agents]
  related_skills: []
---

# 编程工作流

这是一个与具体产品、模型和工具无关的编程项目工作流。它只定义角色契约、任务单、验收证据、子代理协作、恢复和合并规则；具体的 agent runtime、子代理启动方式、后台监督器和通知方式由运行时适配层提供。

## 适用场景

当用户要求 AI agent 修改、重构、修复、扩展或验证 Git 项目时使用，尤其适用于需要隔离工作树、独立审查、真实测试和可恢复后台执行的任务。

不要把“主代理成功返回”当成产品完成，也不要在没有任务单、验收证据或独立审查时直接合并。

## 角色契约

### 用户

- 提供产品目标、优先级和必须由人裁决的选择。
- 在主代理报告真实设计决策、外部授权或不可逆风险时作出裁决。

### 主代理

主代理拥有工作流控制权，但不默认替代执行子代理编写业务代码：

1. 读取项目状态和现有设计事实。
2. 将用户目标拆成可独立验收的任务。
3. 为当前任务编写任务单和逐条验收标准。
4. 提交并冻结任务契约后再派发执行子代理。
5. 调度执行子代理和独立审查子代理。
6. 检查全部当前任务证据，重新执行关键验收。
7. 更新任务单、路线图和当前进度。
8. 只有全部闸门通过后才合并工作树，并在主工作树复验。
9. 遇到真实决策点时保留现场并停止，不自行猜测。

### 执行子代理

- 在指定 worktree 中实现冻结任务单。
- 按红（失败测试）→绿（最小实现）→蓝（重构）循环工作。
- 运行任务单中的验收命令和必要的回归检查。
- 只在自己的任务证据目录写执行报告。
- 不修改冻结的目标、范围和验收标准，不自行扩大产品设计。

### 审查子代理

- 使用独立上下文读取任务单、代码和执行报告。
- 重新运行验收，而不是转述执行子代理的结果。
- 检查实现正确性、边界、范围、任务覆盖率和证据新鲜度。
- 发现任务定义遗漏、环境不具备验收能力或设计冲突时报告 BLOCKED/FAIL。
- 默认只读，不修改产品代码。

### 监督器

监督器不是验收者。它只负责在主代理或子代理长时间运行时提供进程存活、冻结检测、完成通知和可恢复状态；退出码和通知都不能替代验收。

## 不可违反的规则

1. **任务单先于实现**：任务契约必须写入项目并提交后，才能创建执行 worktree 或派发执行子代理。
2. **契约冻结**：实现困难不能直接改写验收标准。设计改变必须记录裁决并创建独立 continuation，保留原任务历史。
3. **验收标准就是测试用例**：每条 AC 必须能指向当前测试文件、用例名、断言、命令和结果边界。
4. **独立审查**：审查子代理必须拥有独立上下文并直接复验当前代码；“记录了别人的结果”不算独立审查。
5. **证据必须属于当前任务**：任务 ID、worktree、branch、测试输出和报告路径必须一致且在当前轮次生成。
6. **真实链路不能被邻近测试替代**：单元、组件、协议、持久化、真实浏览器和真实设备各自证明各自的边界。
7. **合并后复验**：工作树通过不等于主工作树通过；依赖、构建产物和原生模块可能不同。
8. **超时必须有边界**：默认每个测试或单条测试命令不得无限等待；达到默认三分钟应停止并诊断。整套测试可按累计耗时使用更大的明确上限。
9. **决策点必须停下**：缺失产品决策、协议兼容性、权限、外部服务或验收环境时，保留现场并报告，不用实现细节替用户定案。
10. **不要用测试数量代替覆盖率**：绿色数量、退出码、报告文件存在和代码行数增加都不能单独产生 PASS。

## 标准生命周期

### 1. 恢复并核对项目状态

主代理先读取项目入口文档、路线图、当前任务指针、Git 分支/worktree、未提交改动、已有任务证据和运行状态。发现多个活动任务或状态不一致时，先 reconcile，禁止盲目重派或重建现场。

### 2. 规划和拆分

以用户行为或一个可验证能力为任务单位，而不是按目录或技术层水平切割。为任务确定依赖、范围、设计契约、风险、决策点和完整验收矩阵。没有写清测试的验收标准，说明设计尚未完成。

任务之间有产物依赖时顺序执行；只有文件范围和 fixture 完全不重叠、且没有隐含依赖时才允许并行。

### 3. 创建并冻结任务单

默认路径为 `docs/tasks/<task-id>.md`，任务级证据默认路径为 `.workflow/<task-id>/`。主代理创建任务单，初始化验收台账和执行记录，提交任务单，并确认当前主分支和新 worktree 的关系。提交后契约区冻结。

### 4. 执行

执行子代理只在当前任务 worktree 实现。用户可见功能应贯通 UI → 前端/协议 → 业务核心 → 领域事实 → 持久化/投影 → 回显 → 重启恢复；纯基础设施任务必须明确标记为 prerequisite，不得冒充产品闭环完成。

### 5. 独立审查

审查子代理从新上下文开始，先核对任务身份和报告新鲜度，再逐条复验 AC。它既审实现，也审“任务单有没有漏掉必须证明的行为”。审查通过只表示当前工作树满足当前冻结契约，不代表已经合并。

### 6. 主代理最终检查

主代理读取当前任务单、执行报告、审查报告和最终检查报告；抽查高风险测试正文；重新运行关键 AC、全量测试、构建、lint、范围检查和冲突检查。所有 AC 必须有完整证据，才能把任务标记为 ready-to-merge。

### 7. 合并和主工作树复验

提交实现与任务证据，使用 branch ref 合并而不是依赖目录路径。合并后在主工作树重新准备依赖，运行聚焦验收、全量测试、构建、lint 和差异检查。只有主工作树也通过，任务才能标记为已合并。

### 8. 持久化结果

主代理更新当前任务单的验收台账、执行记录、continuation 索引和最终结果；更新路线图及当前任务指针；保留任务级 workflow 证据。完成通知应包含项目、task-id、阶段、分支、合并提交、AC 状态和遗留项。

## 证据目录的最小要求

当前任务的 `.workflow/<task-id>/` 至少包含：

- `executor-report.md`：实现范围、每条 AC 的命令与结果、未完成项。
- `review-report.md`：独立复验的命令、当前测试正文检查、范围和覆盖率结论。
- `final-check.md`：主代理最终检查、当前 branch/worktree 身份、合并前结论。

报告必须写明当前 task-id、worktree、branch、轮次、命令、退出码、关键断言和证据文件。旧任务目录只能作为历史，不能满足当前任务的证据要求。

## 任务规模和 continuation

- 一个任务若包含多个独立领域聚合、十个以上互不共享 fixture 的验收族，或同时覆盖多个平台，先拆成可独立审查的切片。
- 实现失败、证据缺失、环境阻塞和设计改变要区分记录。
- continuation 必须有新的任务 ID、新的任务单和新的证据目录；父任务的失败、裁决和已验收部分不得被覆盖。
- 保留实现和证据现场，禁止为了“重新开始”而无理由删除 worktree、分支或报告。

## 运行时适配层

通用工作流只要求运行时提供以下能力：

- 读取和写入项目文件；
- 执行 Git、构建和测试命令；
- 创建隔离 worktree；
- 调度至少一个执行子代理和一个独立审查子代理；
- 维持任务状态和报告；
- 对长任务提供可恢复监督或明确的人工接管入口。

如果运行时无法提供某项能力，必须在任务结果中标记证据边界，不能把降级路径说成等价验证。

## 参考文件

- `templates/task-sheet.md`：通用任务单。
- `references/task-design.md`：规划、拆分和契约冻结。
- `references/acceptance-evidence.md`：验收矩阵和证据完整性。
- `references/execution-and-review.md`：执行子代理与审查子代理规则。
- `references/merge-and-recovery.md`：监督、恢复、合并和主工作树复验。
- `references/live-and-browser.md`：真实浏览器、设备和多层证据边界。
- `references/multi-medium.md`：跨文件、文档和数据库操作的故障矩阵。
- `references/runtime-adapter.md`：运行时适配层契约。
- `scripts/validate_task_sheet.py`：任务单静态结构校验器。
